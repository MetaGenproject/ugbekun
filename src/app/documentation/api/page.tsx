
"use client";

import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function ApiReferencePage() {
  return (
    <div className="container mx-auto py-10">
      <Card>
        <CardHeader>
          <CardTitle>API Reference</CardTitle>
          <CardDescription>
            This page is currently under construction while a persistent build error is being resolved. The content will be restored shortly.
          </CardDescription>
        </CardHeader>
      </Card>
    </div>
  );
}
